<Page
    x:Class="StormPC.Views.BaseData.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Quản lý sản phẩm" 
                  FontSize="32" 
                  FontWeight="SemiBold" 
                  Margin="20,20" 
                  HorizontalAlignment="Center"
                  Foreground="{ThemeResource SystemAccentColor}"/>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <AutoSuggestBox
                PlaceholderText="Tìm kiếm sản phẩm..."
                QueryIcon="Find"
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                Margin="0,0,10,0"/>
        </Grid>

        <!-- Products GridView -->
        <ScrollViewer Grid.Row="2" Margin="12,0">
            <GridView x:Name="productsGridView"
                     ItemsSource="{x:Bind ViewModel.Laptops, Mode=OneWay}"
                     SelectionMode="Single">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Margin" Value="8" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="380" Height="240" 
                              Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                              BorderBrush="{ThemeResource SystemBaseLowColor}" 
                              BorderThickness="1" 
                              CornerRadius="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Product Image -->
                            <Border Grid.RowSpan="2" Background="#f5f5f7" CornerRadius="12,0,0,12" Padding="12">
                                <Grid>
                                    <Image Source="{Binding Picture}" 
                                           Width="120" 
                                           Height="180" 
                                           Stretch="UniformToFill"/>
                                    <Border Background="{ThemeResource SystemAccentColor}" 
                                            CornerRadius="6" 
                                            VerticalAlignment="Bottom"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding ReleaseYear}" 
                                                   FontWeight="SemiBold" 
                                                   FontSize="11" 
                                                   Margin="6,2" 
                                                   Foreground="White"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <!-- Product Details -->
                            <StackPanel Grid.Column="1" Margin="16,16,16,10">
                                <TextBlock Text="{Binding ModelName}" 
                                           FontWeight="SemiBold" 
                                           FontSize="18"
                                           TextWrapping="Wrap"
                                           MaxLines="2"
                                           Foreground="{ThemeResource SystemAccentColor}"/>

                                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                    <TextBlock Text="Thương hiệu: " FontWeight="SemiBold" Opacity="0.7"/>
                                    <TextBlock Text="{Binding BrandName}" 
                                               Foreground="#0078D7" 
                                               FontWeight="Medium"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <TextBlock Text="CPU: " FontWeight="SemiBold" Opacity="0.7"/>
                                    <TextBlock Text="{Binding CPU}" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="RAM: " FontWeight="SemiBold" Opacity="0.7"/>
                                    <TextBlock>
                                        <Run Text="{Binding RAM}"/>
                                        <Run Text="GB"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="Ổ cứng: " FontWeight="SemiBold" Opacity="0.7"/>
                                    <TextBlock>
                                        <Run Text="{Binding Storage}"/>
                                        <Run Text="GB"/>
                                        <Run Text="{Binding StorageType}"/>
                                    </TextBlock>
                                </StackPanel>

                                <TextBlock Margin="0,8,0,0">
                                    <Run Text="Giá từ: " FontWeight="SemiBold"/>
                                    <Run Text="{Binding LowestPrice}" FontSize="16" Foreground="#0078D7"/>
                                    <Run Text=" VNĐ"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Action Buttons -->
                            <StackPanel Grid.Row="1" Grid.Column="1" 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Right" 
                                        Margin="0,0,12,8">
                                <Button Style="{ThemeResource AccentButtonStyle}"
                                        Margin="0,0,8,0"
                                        Padding="12,6"
                                        CornerRadius="4">
                                    <StackPanel Orientation="Horizontal">
                                        <FontIcon Glyph="&#xE70F;" FontSize="12"/>
                                        <TextBlock Text="Chỉnh sửa" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </Button>
                                <Button Background="#E81123"
                                        Foreground="White"
                                        Padding="12,6"
                                        CornerRadius="4">
                                    <StackPanel Orientation="Horizontal">
                                        <FontIcon Glyph="&#xE74D;" FontSize="12"/>
                                        <TextBlock Text="Xóa" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>

        <!-- Action Buttons -->
        <CommandBar Grid.Row="3" 
                    HorizontalAlignment="Center"
                    Background="Transparent"
                    IsOpen="False"
                    DefaultLabelPosition="Right"
                    Margin="0,15,0,25">
            <AppBarButton Label="Thêm sản phẩm mới" 
                         Icon="Add"
                         Background="{ThemeResource SystemAccentColor}"
                         Foreground="White"/>
        </CommandBar>

        <!-- Loading Indicator -->
        <Grid Grid.RowSpan="4" 
              Background="{ThemeResource SystemAltMediumLowColor}"
              Opacity="0.7"
              Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                         Width="50" 
                         Height="50"/>
        </Grid>
    </Grid>
</Page> 