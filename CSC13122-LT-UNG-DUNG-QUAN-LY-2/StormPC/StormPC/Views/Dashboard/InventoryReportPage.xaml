<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="StormPC.Views.Dashboard.InventoryReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:lvcore="using:LiveChartsCore"
    xmlns:vm="using:StormPC.ViewModels.Dashboard"
    xmlns:helpers="using:StormPC.Helpers"
    xmlns:coreHelpers="using:StormPC.Core.Helpers"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:winui="using:Microsoft.UI.Xaml"
    xmlns:grid="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:NumberFormatConverter x:Key="NumberFormatConverter"/>
        <helpers:CurrencyConverter x:Key="CurrencyConverter"/>
        <Style x:Key="CardStyle" TargetType="Grid">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Báo cáo kho hàng" 
                         Style="{StaticResource TitleTextBlockStyle}"
                         VerticalAlignment="Center"/>

                <CalendarDatePicker Grid.Column="1" 
                                  Header="Từ ngày"
                                  Date="{x:Bind ViewModel.StartDate, Mode=TwoWay}"
                                  Margin="10,0"/>

                <CalendarDatePicker Grid.Column="2" 
                                  Header="Đến ngày"
                                  Date="{x:Bind ViewModel.EndDate, Mode=TwoWay}"
                                  Margin="10,0"/>

                <Button Grid.Column="3"
                        Content="Làm mới"
                        Command="{x:Bind ViewModel.RefreshCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="10,0"/>
            </Grid>

            <!-- KPI Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Tổng sản phẩm" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalProducts, Mode=OneWay}" Style="{StaticResource KPIValueStyle}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="1" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Tổng tồn kho" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalStock, Mode=OneWay}" Style="{StaticResource KPIValueStyle}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="2" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Giá trị tồn kho" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Style="{StaticResource KPIValueStyle}">
                            <Run Text="{x:Bind ViewModel.TotalValue, Mode=OneWay}"/>
                            <Run Text="VNĐ"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="3" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Giá trị TB/SP" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Style="{StaticResource KPIValueStyle}">
                            <Run Text="{x:Bind ViewModel.AverageStockValue, Mode=OneWay}"/>
                            <Run Text="VNĐ"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="4" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Tỷ lệ quay vòng" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Style="{StaticResource KPIValueStyle}">
                            <Run Text="{x:Bind ViewModel.StockTurnoverRate, Mode=OneWay}"/>
                            <Run Text="lần"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="5" Style="{StaticResource KPICardStyle}" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Sắp hết hàng" Style="{StaticResource KPITitleStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.LowStockProducts, Mode=OneWay}" Style="{StaticResource KPIValueStyle}"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Stock Trend Chart -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="400"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Xu hướng tồn kho" 
                         Style="{StaticResource SubtitleTextBlockStyle}"
                         Margin="0,0,0,10"/>

                <lvc:CartesianChart Grid.Row="1"
                    Series="{x:Bind ViewModel.StockTrendSeries, Mode=OneWay}"
                    XAxes="{x:Bind ViewModel.StockTrendXAxes, Mode=OneWay}"
                    YAxes="{x:Bind ViewModel.StockTrendYAxes, Mode=OneWay}"/>
            </Grid>

            <!-- Category and Brand Analysis -->
            <Grid Grid.Row="3" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Category Analysis -->
                <Grid Grid.Column="0" Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="300"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Phân tích theo danh mục" 
                             Style="{StaticResource SubtitleTextBlockStyle}"
                             Margin="0,0,0,10"/>

                    <lvc:CartesianChart Grid.Row="1"
                        Series="{x:Bind ViewModel.CategoryDistributionSeries, Mode=OneWay}"
                        XAxes="{x:Bind ViewModel.CategoryXAxes, Mode=OneWay}"
                        YAxes="{x:Bind ViewModel.CategoryYAxes, Mode=OneWay}"/>

                    <grid:DataGrid Grid.Row="2"
                             ItemsSource="{x:Bind ViewModel.CategoryAnalytics, Mode=OneWay}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             Margin="0,10,0,0">
                        <grid:DataGrid.Columns>
                            <grid:DataGridTextColumn Header="Danh mục" Binding="{Binding CategoryName}"/>
                            <grid:DataGridTextColumn Header="Số SP" Binding="{Binding TotalProducts}"/>
                            <grid:DataGridTextColumn Header="Tồn kho" Binding="{Binding TotalStock}"/>
                            <grid:DataGridTextColumn Header="Giá trị" Binding="{Binding TotalValue}"/>
                            <grid:DataGridTextColumn Header="% Tổng" Binding="{Binding PercentageOfTotal}"/>
                        </grid:DataGrid.Columns>
                    </grid:DataGrid>
                </Grid>

                <!-- Brand Analysis -->
                <Grid Grid.Column="1" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="300"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Phân tích theo thương hiệu" 
                             Style="{StaticResource SubtitleTextBlockStyle}"
                             Margin="0,0,0,10"/>

                    <lvc:CartesianChart Grid.Row="1"
                        Series="{x:Bind ViewModel.BrandDistributionSeries, Mode=OneWay}"
                        XAxes="{x:Bind ViewModel.BrandXAxes, Mode=OneWay}"
                        YAxes="{x:Bind ViewModel.BrandYAxes, Mode=OneWay}"/>

                    <grid:DataGrid Grid.Row="2"
                             ItemsSource="{x:Bind ViewModel.BrandAnalytics, Mode=OneWay}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             Margin="0,10,0,0">
                        <grid:DataGrid.Columns>
                            <grid:DataGridTextColumn Header="Thương hiệu" Binding="{Binding BrandName}"/>
                            <grid:DataGridTextColumn Header="Số SP" Binding="{Binding TotalProducts}"/>
                            <grid:DataGridTextColumn Header="Tồn kho" Binding="{Binding TotalStock}"/>
                            <grid:DataGridTextColumn Header="Giá trị" Binding="{Binding TotalValue}"/>
                            <grid:DataGridTextColumn Header="% Tổng" Binding="{Binding PercentageOfTotal}"/>
                        </grid:DataGrid.Columns>
                    </grid:DataGrid>
                </Grid>
            </Grid>

            <!-- Stock Aging Analysis -->
            <Grid Grid.Row="4" Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="300"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Phân tích tuổi tồn kho" 
                         Style="{StaticResource SubtitleTextBlockStyle}"
                         Margin="0,0,0,10"/>

                <lvc:CartesianChart Grid.Row="1"
                    Series="{x:Bind ViewModel.StockAgingSeries, Mode=OneWay}"
                    XAxes="{x:Bind ViewModel.AgingXAxes, Mode=OneWay}"
                    YAxes="{x:Bind ViewModel.AgingYAxes, Mode=OneWay}"/>

                <grid:DataGrid Grid.Row="2"
                         ItemsSource="{x:Bind ViewModel.AgedInventories, Mode=OneWay}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         Margin="0,10,0,0">
                    <grid:DataGrid.Columns>
                        <grid:DataGridTextColumn Header="SKU" Binding="{Binding SKU}"/>
                        <grid:DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ModelName}"/>
                        <grid:DataGridTextColumn Header="Danh mục" Binding="{Binding CategoryName}"/>
                        <grid:DataGridTextColumn Header="Thương hiệu" Binding="{Binding BrandName}"/>
                        <grid:DataGridTextColumn Header="Tồn kho" Binding="{Binding StockQuantity}"/>
                        <grid:DataGridTextColumn Header="Giá trị" Binding="{Binding StockValue}"/>
                        <grid:DataGridTextColumn Header="Số ngày tồn" Binding="{Binding DaysInStock}"/>
                        <grid:DataGridTextColumn Header="Lần bán cuối" Binding="{Binding LastSoldDate}"/>
                    </grid:DataGrid.Columns>
                </grid:DataGrid>
            </Grid>

            <!-- Restock Suggestions -->
            <Grid Grid.Row="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Đề xuất nhập hàng" 
                         Style="{StaticResource SubtitleTextBlockStyle}"
                         Margin="0,0,0,10"/>

                <grid:DataGrid Grid.Row="1"
                         ItemsSource="{x:Bind ViewModel.RestockSuggestions, Mode=OneWay}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True">
                    <grid:DataGrid.Columns>
                        <grid:DataGridTextColumn Header="SKU" Binding="{Binding SKU}"/>
                        <grid:DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ModelName}"/>
                        <grid:DataGridTextColumn Header="Danh mục" Binding="{Binding CategoryName}"/>
                        <grid:DataGridTextColumn Header="Thương hiệu" Binding="{Binding BrandName}"/>
                        <grid:DataGridTextColumn Header="Tồn kho" Binding="{Binding CurrentStock}"/>
                        <grid:DataGridTextColumn Header="TB bán/tháng" Binding="{Binding AverageMonthlySales}"/>
                        <grid:DataGridTextColumn Header="Đề xuất nhập" Binding="{Binding SuggestedReorderQuantity}"/>
                        <grid:DataGridTextColumn Header="Giá trị nhập" Binding="{Binding EstimatedValue}"/>
                    </grid:DataGrid.Columns>
                </grid:DataGrid>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page> 